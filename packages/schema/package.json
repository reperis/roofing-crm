{
  "name": "@roofing/schema",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod schemas and types for CRM leads and the Chester County records the CRM consumes",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@roofing/tsconfig": "workspace:*",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
